# Project Retrospective 详细参考

> 本文件是 `SKILL.md` 的渐进披露层：仅在需要具体模板、文件结构、维护规则或面试盘问用法时读取。

## 知识库文件结构

知识库位于飞书「面经 / 实习 / cosense」节点下（space_id `7655991149382880216`，cosense 节点 token `MmYQwm7CwiyP2wklb54c1TlTnQh`）。**每个主题 = cosense 下一个独立子文档**，文档标题带 `NN-` 序号保持可读顺序：

| 文档标题 | 内容 |
|------|------|
| `00-项目复盘总览` | 项目宏观：定位、业务、分层、技术栈、一句话讲清「做什么+怎么做」 |
| `01-系统架构` | 各子系统架构图 + 段落说明，记录架构演进 |
| `02-核心链路` | 端到端请求流、事件流、状态机（配流程图） |
| `03-数据Schema` | 数据模型：实体、表/字段、关系、索引、迁移决策 |
| `04-技术选型` | 每个技术选型的七要素决策记录 |
| `05-BadCase` | 历史 Bad Case：症状、根因、修复、可复用教训 |
| `06-权衡Tradeoff` | Trade-off 汇总：取舍双方、代价、为什么接受 |
| `07-性能指标` | 性能指标：P50/P95/P99、QPS、TTFT、端到端延迟 |
| `08-面试盘问` | 面试盘问提纲：递进式追问、高频问题 + 参考答案 |

无需一次性建全，按沉淀内容增量创建即可。

**在 cosense 节点下创建子文档（推荐）：**
```powershell
lark-cli docs +create --doc-format markdown --title "文档标题" --content "@./相对路径.md" --parent-token MmYQwm7CwiyP2wklb54c1TlTnQh
```
`--parent-token` 接收 wiki 节点 token（=cosense 的 `MmYQwm7CwiyP2wklb54c1TlTnQh`），创建后文档直接挂在 cosense 下，成功返回 `data.document.document_id`/`url`。

备选：先 `wiki +node-create --space-id 7655991149382880216 --parent-node-token MmYQwm7CwiyP2wklb54c1TlTnQh --title "标题" --obj-type docx`（返回 node_token/obj_token），再用 `docs` 域读写。

回读验证：`lark-cli docs +fetch --doc <document_id> --doc-format markdown`，确认中文与线条字符无损。

## 决策七要素模板（写作细则）

```markdown
## 决策：<标题，一句话说清选了什么>

**背景**：当时业务/技术面临什么问题，为什么要做这个决策。

**约束**：成本预算、性能上限、团队能力、迁移成本、兼容要求、时间窗。

**候选方案**：
- A：<方案>，优点/缺点
- B：<方案>，优点/缺点
- C：<方案>，优点/缺点

**选择依据**：为什么选 X（给出可量化的对比，如吞吐、延迟、实现成本、可维护性）。

**实现**：落地方式，关键文件/模块，实现过程中踩的坑。

**结果**：上线后效果、性能实测数据、是否达成预期。

**遗留问题**：未解决/可改进/未来风险。
```

## 图例规范（更多示例）

**拆分/多路：**
```
user_id = null?
  │
  ├→ Yes → ProfileIncompleteFlow
  │
  └→ No  → ContinueChatFlow
```

**表格化模块矩阵（横向 + 纵向）：**
```
service A ──────┐
                ├─→ downstream B
service C ──────┘
```

**状态机：**
```
IDLE ──enter──→ MATCHING ──match──→ MATCHED
  ▲                                   │
  └─────────expire────────────────────┘
```

图后固定 1-2 句解读，说明关键节点含义与数据流向。

## 知识库维护规则

1. **去重演进**：新增前先 grep 检查是否已有同一主题。已有 → 更新补全（如补上七要素/图/新结果），不重复开条目。
2. **不可编造**：缺资料的决策要素标「待补充」，留待后续确认，绝不用猜测填充。
3. **版本时间线**：涉及演进（架构变更、方案替换）时，标注时间/版本，说明「旧方案 → 新方案」的迁移路径与原因。
4. **证据来源**：重要结论标注来源（如「线上日志观察到」「压测数据」「PR：xxx」）。
5. **飞行记录**：一条决策若还没定案，可先记入「待总结」，定案后补全。

## 面试盘问用法

知识库的两层消费方式：

1. **项目复盘**：按 `00 → 08` 顺序通读，重建项目全貌。
2. **面试盘问准备**（用户要求时）：
   - 从 `04-tech-choices.md` 抽 3-5 个决策，生成「递进式追问」：先问结论 → 再问为什么 → 再问候选对比 → 再问遗留问题。
   - 从 `05-bad-cases.md` 抽 1-2 个，练习「STAR」式讲法（情境/任务/动作/结果）。
   - 从 `01/02` 抽取架构图，练习「边画边讲」。
   - 在 `08-interview-qa.md` 中维护高频追问与参考答案，随沉淀持续扩充。

## 与 save-to-feishu 的关系

- 本知识库**直接落地在飞书**（cosense 节点下），因此不再需要额外“同步/备份”步骤。
- save-to-feishu 仅用于“把某个本地 md / 一次性内容独立写入飞书文档”的单独场景；本知识库的主题文档统一用 `wiki +node-create`（或 `docs +create` 后 `wiki +move`）挂在 cosense 下。
- 两者共用同一 lark-cli，写入前注意共同避坑项：`@file` 只接受相对路径、先 `[Console]::OutputEncoding = UTF8`、中文乱码属显示层不影响写入、写操作前 `--dry-run` 预览。
